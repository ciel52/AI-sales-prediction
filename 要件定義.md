# 来店・売上予測AI 要件定義書

## 1. 背景・目的

小売・サービス業の **ポイントシステム** に蓄積される購買情報を中心に、天候・人流・SNS などの外部情報を組み合わせて、
**来店予測（主）→ 売上予測（従）** を行う分析AIを構築する。

本ドキュメントは、その第一歩となる **MVP（最小限の実用プロトタイプ）** の要件を定義する。

### ゴール

- 実データが揃う前に、**擬似データ** を使って「良い精度で来店・売上予測ができるか」を検証する
- そのための **分析の型（プロンプト／分析パイプライン）** を確立し、後で実データに差し替えられる状態にする
- データベース連携・自動化は後続フェーズとし、まずは **分析の中身** を固める

### 既存の技術検証（PoC）の位置づけ

- 前段として、イベント・観光分野を題材にした **翌日売上予測の技術検証（**`archive/demo.ipynb`**）** を実施済み。
- 合成データに埋め込んだ傾向を機械学習が学習し、単純ベースライン比で MAPE 約10%（ベースライン約31〜33%）を達成。
「擬似データ → 特徴量 → 学習 → 予測 → 精度評価」の一連が機能することを確認済み。
- 本要件では、この検証済みの型を **小売・サービス業ポイントシステム／来店予測** に作り替える。

---



## 2. 対象ドメインと予測対象


| 項目      | 内容                     |
| ------- | ---------------------- |
| 対象ドメイン  | 小売・サービス業（ポイントシステム運用企業） |
| 主たる目的変数 | **来店予測**               |
| 従たる目的変数 | **売上予測**（来店予測から接続）     |




### 予測の粒度（決定：両方／段階的に実施）

来店予測には大きく2つの捉え方があり、**両方を採用**する。設計が異なるため段階的に進める。


| 区分                 | 内容                          | 主な用途         | 相性の良い説明変数       | 実施      |
| ------------------ | --------------------------- | ------------ | --------------- | ------- |
| **A. 店舗単位・日次（主軸）** | 各店舗の1日の来店客数を予測 → 客単価を介して売上へ | 人員配置・発注・販促計画 | 天候・人流・SNS・カレンダー | フェーズ2・3 |
| **B. 会員個人単位**      | 会員ごとの次回来店・離反（churn）を予測      | クーポン配信・CRM施策 | CSPM購買履歴・RFM    | フェーズ4   |


> まず **A（店舗単位・日次来店客数 → 売上）** を主軸に構築し、型が固まった後に **B（会員個人）** へ展開する。



### 売上予測への接続方法（暫定）

- 売上 ＝ 来店客数 × 客単価 と分解し、来店予測に客単価モデル（曜日・天候・会員構成で変動）を掛けて売上を推定する方式を採用予定。
- 直接「売上金額」を予測する方式とも比較し、精度の良い方を採る。

---



## 3. MVP の定義（決定）

「まずしっかり分析できるプロンプト等の作成をMVPとする」という方針を、
**「分析パイプライン（ノートブック）＋ LLMによる解釈・施策提案プロンプト」** までを MVP として具体化する。

- **MVPに含む**
  - 擬似データの生成（購買・来店・会員・天候・人流・SNS）
  - 来店予測モデル（機械学習）＋ 精度検証
  - 来店 → 売上の接続
  - 分析・予測結果を **解釈し施策提案するLLMプロンプト**（自然言語での説明・打ち手提示）
- **MVPに含まない（後続）**
  - データベース連携・ETL
  - 定期自動実行・ダッシュボード
  - 実データ接続

> ※ 補足：精度の中核は機械学習が担い、LLMプロンプトは「結果の解釈・説明・施策提案」を担う役割分担とする。
> 「プロンプトのみで予測精度を出す」構成ではない点を明記（プロンプトは分析パイプラインを駆動・解釈する位置づけ）。

---



## 4. スコープ



### 含めるもの

- 現実的なパターンを埋め込んだ擬似データ生成
- 特徴量エンジニアリング（データリーク対策込み）
- 来店予測モデルの学習・予測・精度評価（ベースライン比較）
- 来店 → 売上予測への接続
- 予測結果を解釈・説明し、施策を提案するLLMプロンプトの設計



### 含めないもの（後続フェーズ）

- 外部API・DBからの実データ取得・連携
- 定期自動実行・運用基盤・ダッシュボード化
- 会員個人単位の離反予測（オプション、第4フェーズ）

---



## 5. 説明変数（データソース）設計

予測時点で入手可能な情報のみを使用する（**データリーク対策**）。

### 5.0 実データ確認結果（テストデータ実物のEDA）★重要

`test_data/` に提供された実データ（TSV／タブ区切り）を確認した結果、当初の想定と異なる重要点が判明した。

#### 提供物の実態


| データ     | ファイル                                            | 期間                      | 規模                                                     | 主な単位                      |
| ------- | ----------------------------------------------- | ----------------------- | ------------------------------------------------------ | ------------------------- |
| 購買履歴    | `buying_data/buying_history.csv`                | 2024-10-01 〜 2026-06-15 | 2,439,380 伝票 / **187 店舗** / 約 40.7 万会員                 | 伝票（RCPT 241.3万・RETN 2.6万） |
| 購買明細    | `buying_data/buying_history_detail.csv`         | 同上                      | 約 745.6 万明細                                            | 商品明細                      |
| 商品マスタ   | `buying_data/product.csv`                       | -                       | 約 18.6 万商品                                             | 商品（大/中/小分類あり）             |
| 通行人カウント | `flow_counter_data/flow_counter_event_data.csv` | 2025-11-19 〜 2026-06-12 | 53,381 イベント / **端末は実質1台**（`1000000`）+ `UNKNOWN` 6,005件 | 入退室イベント（IN/OUT）           |
| 天候      | （実物データ未提供・**定義のみ**）                             | -                       | -                                                      | -                         |


- **店舗×日のユニーク数（購買）＝ 9,946**。日次×店舗パネルとして扱える。
- **通行人カウント（端末1台のIN）＝ 140日分・IN計25,176・平均約180人/日**。単店の来店実測として扱える規模。
- 購買の `D_TRANSACTION_DT` は時刻が `00:00:00` 固定＝**日付粒度のみ**（POSが時刻を渡していない）。時間帯分析は購買側では不可。
- 通行人カウントの `D_EVENT_DT` は `YYYYMMDDHHMMSS`＝秒単位。単店の**時間帯分析は可能**。



#### ★最重要の論点：来店（通行人）データと購買データが「店舗単位でリンクできない」

- 購買は **187 店舗**、通行人カウントは **端末1台のみ**で、端末→店舗の対応表（`FLOW_COUNTER_DEVICE` / 店舗マッピング）が提供されていない。
- そのため「ある店舗の通行人来店数」と「同じ店舗の購買」を突き合わせる（＝来店→購買のコンバージョン分析）ことが、現データでは **できない**。
- 期間も、購買 2024-10〜、通行人 2025-11〜 と**開始がずれる**（重複は 2025-11〜2026-06 の約7か月）。

> これは以前ご相談いただいた「購買と来店がリンクした本番データがない」懸念が、実データで顕在化したもの。
> **ロジック（パイプライン）は正しく作れば、後で正しく紐づいたデータを入れて活用できる**が、現データでは両者を直結した精度検証はできない。



#### そこで、実データフェーズの目的変数を次のように整理する（方針転換）


| 系統                       | 目的変数                              | データ                  | 位置づけ                                   |
| ------------------------ | --------------------------------- | -------------------- | -------------------------------------- |
| **A. 購買来店・売上（主軸／リンク良好）** | 店舗×日の **購買客数（伝票数/会員数）** と **純売上** | CSPM 購買（187店・良質にリンク） | 実データで精度検証する本命。天候（要実物）・カレンダー・ラグで予測      |
| **B. 通行人来店（単店・独立デモ）**    | 端末1台の **日次入店数（IN集計）**             | FLOW_COUNTER（単店）     | 「通行人→来店」パイプラインを独立に実証。店舗マッピングが来たら A と接続 |


> ※「純粋な通行人来店（店前通過含む全来店）」は B でのみ観測でき、「購買に至った来店」は A で観測できる。
> 両者を結ぶ**コンバージョン**は、端末→店舗マッピング入手後（フェーズ6.5）に扱う。



### 5.1 提供データソース（実テーブル定義に基づく）

共有されたテーブル定義により、実データのソースが判明した。
当初「CPSS」と表記していたが、正しくは **CSPM**（※会員ID自体は `CPSS...:USR` 形式）。また「人流予測」「SNS」の想定は実データと差異があるため下記に修正する。

#### ① CSPM 購買情報（ポイントシステム / POS連動）


| テーブル                         | 内容                                                            | 連携       |
| ---------------------------- | ------------------------------------------------------------- | -------- |
| `CSPM_BUYING_HISTORY`        | 標準の購買履歴。**伝票単位**（POS連動）。売上(RCPT)・返品(RETN)を含み、取消されていないもののみ送られる | 差分（直近5日） |
| `CSPM_BUYING_HISTORY_DETAIL` | 購買履歴の明細（購入商品ごと）                                               | 差分（直近5日） |
| `CSPM_PRODUCT`               | 標準の商品マスタ（オプション扱いのため全レコードがあるとは限らない）                            | 全件       |
| `CSPM_SHOP`                  | 標準の店舗マスタ（同上）                                                  | 全件       |


- 日次に集計して説明変数化：客単価・買上点数・カテゴリ別売上構成・会員来店数/比率・RFM 等。
- **注意①**：返品(RETN)を差し引いて純売上・純客数を算出する。
- **注意②**：差分連携（直近5日）のため、運用は増分更新を前提に設計する。
- **注意③**：会員個人粒度（フェーズ4）は伝票・明細の会員IDで CSPM 内で完結。



#### ② 来店データ（通行人カウントAI / FLOW_COUNTER）★目的変数の出所


| テーブル                      | 内容                             | 連携       |
| ------------------------- | ------------------------------ | -------- |
| `FLOW_COUNTER_EVENT_DATA` | 通行人カウントAIが検知した**入退室イベントの生データ** | 差分（直近5日） |


- これを日次集計したものが **来店客数（目的変数B）** になる。実測カウントのため代理指標は不要。
- **実データの実態**：端末は実質1台（`1000000`）。IN 28,100 / OUT 25,281 と数が一致せず、`UNKNOWN` 端末が 6,005 件ある。日次はIN集計で約180人/日、データのある日数は140日（期間207日中）で**欠損日がある**。
- **集計ルール（要確定）**：来店数は原則 `C_EVENT_TYPE = IN` の日次カウントを採用。`UNKNOWN` 端末の扱い（除外/合算）、欠損日補完、営業時間の扱いを決める必要がある。
- **要確認**：この端末がどの店舗（CSPMの187店のどれか）に対応するか＝**端末→店舗マッピング**（現状不明）。



#### ③ 天候データ（FLOW_COUNTER_DAILY_WEATHER）


| テーブル                         | 内容                                                                      | 連携       |
| ---------------------------- | ----------------------------------------------------------------------- | -------- |
| `FLOW_COUNTER_DAILY_WEATHER` | 気象データ。**OpenWeatherMap One Call API v3 の Daily Aggregation** のレスポンスに基づく | 差分（直近5日） |
| `FLOW_COUNTER_WEATHER_LOC`   | 上記の集計対象地点                                                               | 全件       |


- 気温・降水などを来店予測の説明変数に利用。列は雲量・湿度・降水量・気圧・最低/最高/時間帯別気温・風速/風向・生JSONペイロード。
- **実データの実態**：今回の提供物には**天候の実データCSVは含まれず、テーブル定義のみ**。実データ検証には別途エクスポートが必要。
- **注意（リーク）**：Daily Aggregation は基本「実績（その日の集計）」。翌日予測に使うには **予報値** が必要。
同 API の forecast を別途取得する設計にする（実績天候を当日特徴に使うと本番でリーク）。



#### ④ 人流（外部の独立した「人流予測」データは提供テーブルに無い）

- 当初想定した「店舗周辺の人出予測（Agoop 等の外部データ）」は提供テーブルに含まれない。
- 来店予測の文脈では、FLOW_COUNTER は**来店そのものの実測**であり、当日値を特徴量に使うのは循環/リーク。
→ **過去の来店ラグ・移動平均**として利用する（独立した先行指標ではない）。



#### ⑤ SNS情報（今回の提供テーブルには無し）★方針決定済（2026-08-14）

- 共有されたテーブルに SNS のソースは含まれていない。
- 現状の擬似データは SNS を「翌日来店の先行指標」として使っているが、**実データでは未提供**。
- **決定：SNS由来の情報は「学習用の特徴量」には使わず、「LLM施策提案のコンテキスト」として使う。**

##### なぜ学習に使わないか

| 理由                | 内容                                                                                                                        |
| ----------------- | ------------------------------------------------------------------------------------------------------------------------- |
| **過去分が遡れない**      | 学習期間は購買データに合わせて 2024-10〜2026-06。SNSは直近しか取得できず、学習・検証に投入できない（最大の制約）                                                        |
| **統計的に学習できない形**   | 祭り・催事は2年で数回しか起きず、毎回内容が違う。機械学習は「同じ条件の反復観測」から効果を推定するため、係数を推定できない                                                          |
| **擬似データの重要度は根拠外** | `archive/retail_synthetic_data.py` はSNS言及数を「翌日の潜在需要×販促×バズ」から逆算生成しているため、フェーズ2でSNSの重要度が高く出たのは循環論法。実データの根拠にはならない                    |
| **目的変数との相性**      | `receipts` は売上伝票数で `members`（会員ユニーク数）との相関が 0.995＝ほぼ会員カード利用の購買。SNS経由で流入した非会員の来店は `receipts` に乗りにくい                         |
| **効果の上限が小さい**     | 実データモデルの重要度は `receipts_lag7` 0.49 / `shop_id_code` 0.35 / `dow` 0.12 で上位3つがほぼ全て。通常期MAPE 7.8% はベースライン（7日前実績）9.2% との差が既に1.4pt |

##### なぜLLMコンテキストが適切か

- **役割分担に沿う**：`src/llm_advisor.py` の設計方針どおり「数値予測＝ML／解釈・施策提案＝LLM」。LLMは学習不要で、一般知識から「近隣で花火大会 → 夕方の人出増だが駐車場混雑」と推論できる
- **運用リスクが小さい**：特徴量として足すと再学習・再検証が必要で、情報が誤れば予測値そのものが壊れる。コンテキスト注入なら数値予測は無傷で、即時投入・即時撤去が可能
- **効く局面が明確**：年末（12月後半）の来店MAPEは 29%、検証後半だけで 47%。曜日・祝日フラグで表現できない営業スケジュールの断絶であり、まさにイベント情報が埋めるべき穴

##### 二段構えの方針

| 情報の種類                                              | 置き場所            | 狙い                            |
| -------------------------------------------------- | --------------- | ----------------------------- |
| SNS・ニュース由来の**不確実なイベント情報**（地域の祭り、近隣施設の催事、交通規制など）     | **LLMコンテキスト**   | 施策提案の質を上げる。予測の上振れ／下振れ方向を定性的に警告 |
| 社内で**確定情報として持てるもの**（営業日・休業日・営業時間、初売り、自社催事、年末年始スケジュール） | **学習用の特徴量**（後続） | 年末のMAPE悪化を直接改善する              |

> ※ 注意：LLMコンテキスト方式は**提案の質を上げるがMAPEは改善しない**。年末の来店MAPE 29% は残る。
> 精度指標を動かすのは下段（社内確定イベントの特徴量化）の役割であり、両者を混同しないこと。



### 5.2 追加提案データ（あると精度が上がる）

小売・サービス業／ポイントシステムで効きやすい変数を提案する。採否は相談のうえ決定。

- **カレンダー系**：曜日、祝日、連休、月初/月末、給料日・年金支給日、学校休暇
- **販促系**：チラシ配布日、特売・セール、クーポン配信/利用、キャンペーン、ポイント◯倍デー
- **価格系**：主要カテゴリの価格・値引き率
- **会員系**：会員数推移、会員ランク構成、入会経過月、来店間隔の分布
- **競合・地域系**：近隣競合のセール、地域イベント・祭り、スポーツ開催
- **マクロ系**：季節性、長期トレンド、物価・消費マインド

> 特に **ポイント◯倍デー・クーポン配信・特売・給料日** は来店に直結しやすく、優先的に検討したい。



### 5.3 イベント情報のデータ構造（LLMコンテキスト用）★設計済・未実装

5.1 ⑤ の決定に基づき、SNS等から取得したイベント情報を **LLM施策提案のコンテキスト** として渡すための構造を定義する。
学習用の特徴量ではないため、**モデルの入力にはしない**。

#### 5.3.1 保管形式

- 当面は **`data/events.csv`**（1行＝1イベント。UTF-8）。将来的に件数が増えたらPostgreSQLへ移行
- 予測モデルとは独立したファイルとし、パネルデータ（`real_store_daily.csv`）と結合しない

#### 5.3.2 スキーマ

| 列                    | 型 / 取りうる値                                                                                       | 必須 | 説明                                                          |
| -------------------- | ----------------------------------------------------------------------------------------------- | -- | ----------------------------------------------------------- |
| `event_id`           | string                                                                                          | ○  | 一意ID。重複登録の検知に使う                                             |
| `date_start`         | `YYYY-MM-DD`                                                                                    | ○  | 開始日。単日イベントは `date_end` と同値                                   |
| `date_end`           | `YYYY-MM-DD`                                                                                    | ○  | 終了日                                                         |
| `name`               | string                                                                                          | ○  | イベント名（例：青梅花火大会）                                              |
| `category`           | `festival` / `local_event` / `sports` / `mall_event` / `own_event` / `closure` / `traffic` / `alert` / `other` | ○  | 区分。`closure`＝休業・営業時間変更、`traffic`＝交通規制・工事、`alert`＝警報・災害       |
| `scope`              | `shop` / `area` / `all`                                                                         | ○  | 適用範囲                                                        |
| `shop_ids`           | string（`;` 区切り）                                                                                 | △  | `scope=shop` のとき必須                                          |
| `area`               | string                                                                                          | △  | `scope=area` のとき必須（エリア名）                                     |
| `expected_direction` | `up` / `down` / `unknown`                                                                       | ○  | 来店への想定影響の向き                                                 |
| `expected_magnitude` | `high` / `mid` / `low` / `unknown`                                                              | ○  | 想定影響度。注入時の優先順位に使う                                           |
| `source`             | `sns` / `official` / `internal` / `news` / `manual`                                             | ○  | 出典。`official`＝自治体・主催者公式、`internal`＝社内・自社告知                   |
| `confidence`         | `confirmed` / `likely` / `unconfirmed`                                                          | ○  | 確度。**LLMへの断定可否を制御する最重要項目**                                  |
| `note`               | string                                                                                          | -  | 補足（1〜2文まで。長文はプロンプトを圧迫する）                                    |
| `url`                | string                                                                                          | -  | 出典URL                                                       |
| `collected_at`       | ISO8601                                                                                         | ○  | **取得日時**。「いつ時点で知り得たか」を担保する（下記 5.3.5 参照）                      |

#### 5.3.3 LLMへ渡すイベントの選定ルール

`POST /chat` のコンテキスト生成時に、次の条件で絞り込む。

1. **期間の重なり**：日次は対象日、週次／月次は対象期間と `date_start`〜`date_end` が重なるもの
2. **店舗の適用**：`scope=all` ／ `scope=area` かつ店舗のエリア一致 ／ `scope=shop` かつ `shop_ids` に対象店舗を含む
3. **時点整合**：`collected_at` が予測実行時刻以前のもの
4. **件数上限**：`expected_magnitude` 降順 → `confidence` 降順で最大 **5件**（プロンプト膨張とコスト増を防ぐ）

> SNSの生投稿はそのまま渡さない。必ず上記スキーマへ正規化してから注入する。

#### 5.3.4 プロンプトへの反映

`src/llm_advisor.py` の `render_user_prompt()` に新しい節を追加する。
既存の「## SNS動向（前日まで）」（`SNS言及_前日` / `SNS言及_7日平均` / `SNS急増`）は擬似データのバズ指標用であり形が違うため、**流用せず別節を新設する**。

```
## 周辺イベント・告知（判明分 / 予測モデル未反映）
- 2024-12-14〜12-15 青梅花火大会（地域行事・徒歩圏）: 来店増の可能性 / 影響度:高 / 出典:SNS・未確認
- 2024-12-15 隣接道路の工事による交通規制（交通規制）: 来店減の可能性 / 影響度:中 / 出典:公式・確定
```

`SYSTEM_PROMPT` には次の指示を追加する。

- **予測値はイベントを織り込んでいない**ことを前提に、上振れ／下振れしやすい方向を定性的に補足する（数値は書き換えない）
- `confidence` が `confirmed` でない情報は断定せず、「SNS上の情報で未確認ですが」と前置きする
- 該当イベントが無い場合は、この話題に触れない

> 補足：予測値が「イベントを知らない数値」である以上、LLMは**モデルが読み違えそうな日を警告する層**として機能する。
> 制約（学習に使えない）を、そのまま機能（予測の弱点の明示）に変換する設計。

#### 5.3.5 将来の学習利用に向けた布石

`collected_at` を必須にしているのは、将来この情報を特徴量として評価する可能性に備えるため。
「予測実行時点で知り得た情報だけ」を再現できる（point-in-time correct）ため、**リークのない後方検証**が可能になる。

- 今から蓄積を始めれば、1〜2年後には現在欠けている「過去分」が手元に揃う
- そのうえで `source=official` / `internal` かつ `confidence=confirmed` のものに限定すれば、特徴量化の候補になる
- つまり本設計は、ほぼ追加コストなしで**将来の選択肢を確保する**位置づけ

---



## 6. 擬似データ仕様


| 項目     | 内容                                            |
| ------ | --------------------------------------------- |
| 期間     | 2年分（約730日）                                    |
| 粒度     | 日次 × 店舗（**MVPはまず1店舗**）                        |
| 主テーブル  | 来店客数・売上・客単価 / 天候 / 人流 / SNS / カレンダー・販促 / 会員集計 |
| 埋め込む傾向 | 曜日周期・季節性・祝日/給料日効果・天候影響・人流連動・SNS先行効果・販促効果・ノイズ  |


> ※ 現行の擬似データは上記の想定スキーマ。実データ（CSPM / FLOW_COUNTER）接続時（フェーズ6）に、
> 提供テーブルの列・粒度へ合わせて読み込み・日次集計処理を実装する。SNS・外部人流は提供データに無いため、
> 実データ版では当面これらの特徴量を外す前提（採否は「確認事項」参照）。

> 擬似データは「現実に起こりうる関係性」を意図的に埋め込み、モデルがそれを学習・再現できるかで妥当性を検証する。

---



## 7. モデル・評価設計


| 項目       | 内容                                                   |
| -------- | ---------------------------------------------------- |
| メインモデル   | HistGradientBoostingRegressor（勾配ブースティング系。多特徴量・欠損に強い） |
| 比較ベースライン | 前年同日 / 直近移動平均 / 曜日平均                                 |
| 将来検討     | LightGBM、Prophet（季節性）、来店×客単価の階層モデル                   |




### 評価設計

- **時系列分割**（過去で学習 → 未来で検証。ランダム分割は禁止）
- 指標：MAE / RMSE / MAPE ＋ **繁忙日（来店上位）に絞った誤差**
- ベースライン比で機械学習が有意に良いことを確認
- 特徴量重要度で「何が来店を動かすか」を解釈

---



## 8. 処理フロー

```
1. 擬似データ生成（購買/来店・天候・人流・SNS・販促・会員）
        ↓
2. 特徴量エンジニアリング（リーク対策込み）
        ↓
3. 時系列分割（学習 / 検証）
        ↓
4. 来店予測モデル学習
        ↓
5. 来店予測 → 客単価モデル → 売上予測
        ↓
6. 精度評価（ベースライン比較・繁忙日誤差・特徴量重要度）
        ↓
7. LLMプロンプトで結果を解釈・施策提案（自然言語アウトプット）
```

---



## 9. プロジェクトの進め方（フェーズ）


| フェーズ             | 内容                                                                                             | 状態         | 成果物                                                                                                         |
| ---------------- | ---------------------------------------------------------------------------------------------- | ---------- | ----------------------------------------------------------------------------------------------------------- |
| 0. 要件・データ定義      | 本書の確定、データソース項目の確認                                                                              | 完了         | `要件定義.md`                                                                                                   |
| 1. 擬似データ生成       | 小売ポイントシステム想定のデータ生成                                                                             | 完了         | `archive/retail_synthetic_data.py` / `archive/phase1_data.ipynb` / `archive/retail_synthetic.csv`                          |
| 2. 来店予測MVP       | モデル学習・精度検証（ベースライン比較）                                                                           | 完了         | `archive/phase2_visit_model.ipynb`                                                                                  |
| 3. 売上予測接続        | 来店×客単価 → 売上、精度検証                                                                               | 完了         | `archive/phase2_visit_model.ipynb`（同ノートブック内）                                                                        |
| 4.（任意）会員個人予測     | 離反・次回来店予測（CRM向け）                                                                               | 任意         | -                                                                                                           |
| 5. LLM施策提案       | 予測結果の解釈・打ち手提示プロンプト                                                                             | 完了         | `src/llm_advisor.py` / `archive/visit_forecast.py` / `archive/phase5_llm_advisor.ipynb`                                 |
| 6. 実データ取込・EDA    | 提供TSVの取込・クレンジング・日次パネル化・傾向確認                                                                    | **完了**     | `src/real_data_etl.py` / `archive/phase6_real_eda.ipynb` / `data/real_store_daily.csv` / `data/real_flow_daily.csv` |
| 6.5 実データ予測（A系統）  | 購買から店舗×日の購買客数・売上を予測（プール方式・天候なし）。連続日次な密店舗86店に加え、疎な101店舗も「直近実績ラグ＋空白日数」「カレンダーのみ」の2方式で全187店舗プールに拡張 | **完了**     | `src/real_visit_forecast.py` / `archive/phase6_5_real_forecast.ipynb`                                               |
| 6.6 実データ予測（B系統）  | 通行人カウント単店の日次入店数を予測（独立デモ）                                                                       | 後続         | -                                                                                                           |
| 6.9 Webアプリ（本番UI） | 予測操作UI＋施策提案チャット。**Next.js + FastAPI**。日次/週次/月次の選択実行、AIとの対話型施策提案。**学習期間外の任意日付も予測可**（12.7）           | **最小UI完了** | `api/` / `web/` / `要件定義.md` 12章                                                                             |
| 6.95 イベント情報のLLM活用 | SNS等から取得したイベント情報を施策提案AIのコンテキストへ注入（学習用特徴量にはしない）。データ構造・選定ルール・プロンプト設計を確定                             | **設計済・未実装** | `要件定義.md` 5.1⑤ / 5.3                                                                                        |
| 7.（任意）継続学習・施策最適化 | 定期実行ループでの再学習・ドリフト検知＋強化学習/バンディットによる施策改善                                                         | 任意         | -                                                                                                           |


---



## 9.5 フェーズ7（任意）：継続学習と施策の自動改善

定期実行（日次バッチ等）の中で、システムが「使うほど賢くなる」仕組みを検討する。
**用途を2系統に分けて考えるのが重要**で、適した技術が異なる。

### A. 予測精度の向上 → 強化学習ではなく「継続学習」

来店・売上予測は「翌日に正解が判明する」**教師あり学習**のため、強化学習は不向き。
精度を維持・向上させる正攻法は次の通り。

- **定期再学習（リトレーニング）**：新しい実績を蓄積し、モデルを学習し直して季節・トレンド変化に追従
- **データドリフト検知**：予測のずれを監視し、再学習や見直しのトリガーにする
- **オンライン学習**（任意・高度）：データ到着のたびに少しずつ更新



### B. 施策提案の改善 → 強化学習／文脈付きバンディットが適合

施策は「行動を選び、結果（報酬）から良い選び方を学ぶ」問題であり、強化学習の発想が合う。


| 要素         | 例                              |
| ---------- | ------------------------------ |
| 行動（action） | 発注量、人員シフト、クーポン配信先、値引き率、ポイント倍率日 |
| 報酬（reward） | 粗利、廃棄ロス削減、来店・再来店、会員維持率         |
| 方策（policy） | 状況（天候・曜日・在庫・会員）に応じた最適行動の選び方    |


ただし純粋な強化学習は実務ハードルが高い（探索コスト・報酬遅延・非定常）。
**現実的な落とし所は「文脈付きバンディット（Contextual Bandit）」**で、
クーポン・オファー最適化を、損失を抑えつつ学習する用途に適する。

### 前提条件：効果測定ログ

A・Bいずれも、**「施策を実行した結果どうだったか」を記録する効果測定の仕組み** が前提。
現MVPは「予測→提案」までで実行結果ログが無いため、フェーズ6（DB連携・基盤）と密接に関係する。

### 推奨ステップ（段階的導入）

1. 効果ログ基盤（提案・実行・結果の記録）
2. 予測の再学習ループ＋ドリフト検知（＝強化学習ではない）
3. A/Bテスト・効果測定で施策の良し悪しを可視化
4. 文脈付きバンディットでクーポン・オファー最適化（実用的な入口）
5. 本格的な強化学習（在庫・人時の動的最適化など）はデータ・安全性が整ってから慎重に

> デモ候補：①クーポン最適化の文脈付きバンディット、②再学習＋ドリフト検知ループ。

---



## 10. 成功基準（MVPのゴール）

- 擬似データに埋め込んだ来店の傾向を、モデルが学習・再現できている
- 単純ベースラインより機械学習の来店予測精度が有意に良い
- 来店予測から売上予測へ妥当に接続できている
- 予測時点で入手可能な情報のみでリークなく予測できている
- 結果をLLMプロンプトで分かりやすく解釈・施策提案できる
- 本番（実データ）設計に向けた課題・必要データが洗い出せる

---



## 12. 本番活用の基本仕様（Webアプリ）★決定

分析パイプライン（フェーズ6.5まで）を、現場が操作できる **Webアプリ** として提供する。
フェーズ6.9の成果物はこの仕様に沿う。

### 12.1 基本仕様（必須）


| #   | 機能             | 内容                                                 |
| --- | -------------- | -------------------------------------------------- |
| A   | **粒度選択＋予測実行**  | 画面上で **日次 / 週次 / 月次** をボタン（または同等UI）で選択し、予測を実行できる   |
| B   | **施策提案AIチャット** | ユーザーが施策提案AIと **チャット形式** で会話し、予測結果を踏まえた打ち手を対話的に得られる |
| C   | **任意期間の予測**    | **学習データの期間内に限らず、いつの日付でも予測できる**（下記 12.7 参照）          |




### 12.2 画面・操作の想定

```
┌─────────────────────────────────────────────────────────┐
│  [店舗選択]  [日次] [週次] [月次]  [予測実行]              │
├──────────────────────────────┬──────────────────────────┤
│  予測結果パネル                │   施策提案AI チャット       │
│  ・グラフ（実績 vs 予測）       │  ・予測コンテキスト付き      │
│  ・店舗別一覧・KPI             │  ・質問・深掘り・比較        │
│  ・信頼度 / 注意フラグ          │  ・提案の保存・エクスポート   │
└──────────────────────────────┴──────────────────────────┘
```

- **左（または上）**：予測操作と結果可視化
- **右（または下）**：チャット。選択中の店舗・粒度・予測期間が自動で文脈として渡る



### 12.3 粒度の定義（推奨）


| 粒度     | 予測の意味                  | 主な用途           |
| ------ | ---------------------- | -------------- |
| **日次** | 店舗×日の来店客数・売上（現行モデルの主軸） | 人員配置・当日オペレーション |
| **週次** | 日次予測の週合計、または週次モデル      | 発注・シフト・販促計画    |
| **月次** | 日次予測の月合計、または月次モデル      | 予算・在庫・人員計画     |


> 当面は **日次モデルを基本** とし、週次・月次は日次予測の集計で出す方式が実装コストと整合性が高い。
> 疎な店舗向けは、既存のカレンダーのみ／asofラグ方式をバックエンドで利用する。



### 12.4 追加で入れるとよい機能（推奨）

必須ではないが、本番利用の実用性・信頼性を大きく上げるため優先度付きで提案する。


| 優先    | 機能                     | 理由                                       |
| ----- | ---------------------- | ---------------------------------------- |
| **高** | **店舗（複数可）の選択・絞り込み**    | 187店を一度に見ると使えない。エリア／お気に入り店舗が必要           |
| **高** | **チャットへの予測コンテキスト自動注入** | 「この予測を見て」と毎回説明しなくてよい。選択中の店舗・期間・数値をLLMに渡す |
| **高** | **実績 vs 予測の事後比較**      | 「昨日の予測は当たったか」が見えないと信頼されない                |
| **高** | **異常店舗アラート**           | MAPE急増・急落店舗を一覧／通知（前回の外れ店舗分析を運用に接続）       |
| **中** | **予測の不確実性表示**          | 点予測だけでなく「幅」や繁忙日注意フラグがあると現場判断がしやすい        |
| **中** | **会話・提案の履歴保存**         | 誰が何を提案し、どう判断したかの監査・振り返り                  |
| **中** | **CSV / PDF エクスポート**   | 本社報告・店舗への共有                              |
| **中** | **権限（店舗／エリア／本社）**      | 見える店舗を役割で制限                              |
| **低** | **夜間バッチで事前計算＋ボタンは再計算** | ボタン連打でも速く、最新データでの再計算も可能                  |
| **低** | **施策実行ログ（実行した／しない）**   | フェーズ7（効果測定・バンディット）への布石                   |




### 12.5 技術スタック（決定）★

現行資産が **Python（pandas / scikit-learn / Jupyter / llm_advisor）** である点と、
本番UIの中核が **「予測操作」＋「チャット対話」** である点を前提に、次を採用する。

#### 採用構成（2026-07-11 確定：案A）


| 層               | 採用                                                          | なぜこれを選ぶか                                                                                                                       |
| --------------- | ----------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------ |
| **API / 予測サーバ** | **FastAPI（Python）**                                         | ML・特徴量生成がすべてPython。既存 `real_visit_forecast.py` / `llm_advisor.py` をそのまま呼び出せる。OpenAPIでフロントと契約を明確化できる。チャット・予測を同じプロセス／同じサービス群で扱える |
| **フロントエンド**     | **Next.js（React + TypeScript）**                             | 「チャット形式」が本命機能のため、メッセージUI・ストリーミング表示・店舗切替との連動が作りやすい。本番っぽい画面・権限UIにも伸ばしやすい。予測ボタン群との左右レイアウトにも適合                                     |
| **予測エンジン**      | 既存 scikit-learn（`HistGradientBoostingRegressor`）＋ joblib 保存 | すでに精度検証済み。エンジンを差し替えず、まず「使えるUI」に注力する                                                                                            |
| **施策AI**        | 既存 `llm_advisor.py` をチャットAPI化（OpenAI互換API想定）                | プロンプト資産を再利用。APIキー無し時は現行どおりルールベース下書きも可                                                                                          |
| **データ保存（当面）**   | **Parquet / CSV**（現行パネル）                                    | まだDB必須ではない。予測・デモを早く動かせる                                                                                                        |
| **データ保存（本番化時）** | **PostgreSQL**                                              | 予測結果・チャット履歴・ユーザー権限を永続化する段階で導入                                                                                                  |
| **認証（本番化時）**    | 簡易ログイン → 必要なら社内SSO                                          | 最初はデモ用の共有パスワードでも可。店舗権限は後続                                                                                                      |
| **デプロイ**        | Docker（API + Web）                                           | 環境差を減らす。クラウド／オンプレどちらにも載せやすい                                                                                                    |


```
[ブラウザ: Next.js]
        │  REST / SSE（チャットストリーム）
        ▼
[FastAPI]
   ├─ /predict  → real_visit_forecast（日次推論＋週次/月次集計）
   └─ /chat     → llm_advisor（予測コンテキスト＋会話履歴）
        │
        ▼
[データ] Parquet/CSV →（本番化）PostgreSQL
```



#### 採用しなかった案（参考）


| 案                   | 構成                  | 不採用理由                           |
| ------------------- | ------------------- | ------------------------------- |
| Streamlit単体         | PythonのみでUI完結       | 立ち上げは速いが、チャットUX・権限・製品UIの伸びしろが不足 |
| FastAPI + Streamlit | APIは切るがUIはStreamlit | 本命の対話体験を作りにくい                   |




#### 採用しない（または後回し）もの


| 技術                      | 理由                                       |
| ----------------------- | ---------------------------------------- |
| Django                  | 予測API＋チャットには FastAPI の方が軽く、非同期・OpenAPI向き |
| LightGBM/XGBoost への即時移行 | 精度改善余地はあるが、UI構築と同時にやると焦点が散る              |
| 本格的な特徴量ストア（Feast等）      | 店舗数・更新頻度に対して現状は過剰                        |
| 独自チャット基盤                | まずLLM API＋自前プロンプトで十分                     |




### 12.6 フェーズ6.9の進め方

1. **API化（FastAPI）**：ノートブックの予測・LLMを `POST /predict` / `POST /chat` に切り出す
2. **最小Web UI（Next.js）**：粒度ボタン＋予測結果表示＋チャット（店舗は1〜数店から）
3. **推奨機能の追加**：店舗選択、実績比較、異常アラート
4. **認証・権限・PostgreSQL・履歴**：本番運用に必要なガードレール

### 12.7 任意期間の予測と実績比較 ★実装済（2026-08-16）

実績データは 2024-10-01〜2024-12-30 しか無いが、要件上は任意の期間を予測できる必要がある
（12.1 の機能C）。日付ごとに使えるモデルを自動で切り替え、どれを使ったかを必ずレスポンスと
画面に出す。

| モデル            | 使う場面                              | 精度の目安                |
| -------------- | --------------------------------- | -------------------- |
| `lag`          | 前日・前週の実績が揃う日                      | 来店MAPE 約8%（通常期）      |
| `calendar`     | 実績は無いが、対象月が学習データに含まれる（現データの10〜12月） | 来店MAPE 約9%（全187店・参考） |
| `calendar_dow` | 対象月が学習データに無い（現データの1〜9月）            | 季節変動を反映しない**参考値**    |

`calendar_dow` を別に用意しているのは、木モデルが未学習の月を「学習済みの端の月」として
扱うため、月・週番号を入れたままだと**大きく下振れする**から。10〜12月のみのデータで2月を
予測すると、日曜の実績平均50に対し21（-57%）を返した。月・週番号を外すと52になる。

**実績の扱い**

- 予測対象日そのものの実績があれば表示する（学習期間内の日付＝精度検証になる）。
- 無ければ、**同じ月日の過去実績**を前年 → 前々年 → 3年前の順に探して参考値として併記する。
  例：2025-10-12 を予測すると 2024-10-12 の実績を出す。どれも無ければ予測のみ表示。
- 参照日は**曜日が一致しない**（上の例では日曜 vs 土曜）。曜日は来店数への影響が大きい特徴量の
  ため、画面・LLMプロンプトの両方で参照日の曜日を明示し、単純比較しないよう促す。

**残る課題**：現データは10〜12月のみなので、1〜9月は季節性を持たない参考値にとどまる。
通年分の実績が揃えば `calendar` の適用範囲が全月に広がり、この制約は自然に解消する。

---



## 11. 確認事項（オープンクエスチョン）



### 決定済み

- 予測の粒度：**両方**（店舗単位・日次を主軸 → 会員個人は後続フェーズ）
- MVPの成果物：**分析パイプライン（ノートブック）＋ LLM施策提案プロンプト**（擬似データで完成済み）
- データソース判明：購買＝**CSPM**、来店＝**FLOW_COUNTER 入退室イベント**、天候＝**FLOW_COUNTER_DAILY_WEATHER**（提供テーブル定義より）
- 実データは **TSV（タブ区切り）**。購買は187店・2024-10〜2026-06、通行人は単店・2025-11〜2026-06。
- **本番UIの基本仕様**（2026-07-11）：Webアプリで **日次/週次/月次を選択して予測**、および **施策提案AIとチャット形式で会話**（詳細は12章）
- **技術スタック**（2026-07-11）：**Next.js + FastAPI**（案A）を採用。予測は既存scikit-learn、データは当面Parquet/CSV→本番化でPostgreSQL（詳細は12.5）
- **SNS情報の使い道**（2026-08-14）：SNSは過去分を遡って取得できないため**学習用の特徴量には使わない**。イベント情報として**LLM施策提案のコンテキスト**に注入する。社内で確定情報として持てる暦・営業日・自社催事は別途特徴量化して年末精度の改善に充てる（詳細は5.1⑤ / 5.3）



### 要判断（実データ検証を進める前の重要決定）★

1. **来店↔購買の店舗リンク**：通行人カウント端末（1台）が CSPM の187店のどれか＝**端末→店舗マッピング**を提供いただけるか。無い場合は A系統（購買）と B系統（通行人）を**別々**に検証する方針でよいか。
2. **実データフェーズの主目的変数**：A（購買から店舗×日の購買客数・売上）を本命に据える方針でよいか。
3. **天候の実データ**：`FLOW_COUNTER_DAILY_WEATHER` の**実物CSVエクスポート**をいただけるか（現状は定義のみ）。翌日予測用の**予報値**取得可否も。
4. **対象店舗の絞り込み**：187店の中から検証対象を選ぶか（データが連続している店舗を優先）、全店で汎用モデルにするか。



### 未確定（従来からの継続確認）

1. **イベント情報の収集・運用体制**（SNSの使い道自体は決定済 → 5.1⑤）：誰が `data/events.csv` を登録・レビューするか。SNS由来の未確認情報を店舗向け提案に出す際の社内承認ルール。店舗→エリアのマスタ（`scope=area` の判定に必要）を用意できるか
2. **社内確定イベントの提供**：営業日・休業日・営業時間、初売り、自社催事、年末年始スケジュールの**過去分**をいただけるか（学習用特徴量として年末精度の改善に使う）
3. **FLOW_COUNTER 集計ルール**：`IN` のみで来店数とするか、`UNKNOWN` 端末の扱い、欠損日補完、営業時間の扱い
4. **CSPM**：返品(RETN)の控除ルール（純売上・純客数の定義）、会員/非会員（`V_ACCOUNT_ID` が null）の扱い
5. **カテゴリ分類**：`CSPM_PRODUCT` の大/中/小分類を説明変数化する際の粒度
6. 「人流予測」（外部の周辺人出データ）を別途入手するか（提供データには無い）
7. 追加提案データ（給料日・販促・クーポン等）の実データ有無・採否

---



## 改訂履歴


| 日付         | 内容                                                                                                                                                                                                                                                                                                |
| ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2026-06-09 | 初版作成（イベント・観光／売上予測デモ）                                                                                                                                                                                                                                                                              |
| 2026-06-14 | 小売・サービス業ポイントシステム／来店予測へ全面改訂                                                                                                                                                                                                                                                                        |
| 2026-06-14 | 粒度（両方）・MVP定義・店舗数（1店舗）を確定                                                                                                                                                                                                                                                                          |
| 2026-06-14 | フェーズ1完了：販促含む擬似データ生成・傾向確認ノートブックを追加                                                                                                                                                                                                                                                                 |
| 2026-06-14 | フェーズ2・3完了：来店予測モデル（MAPE約9%）と売上予測接続を実装                                                                                                                                                                                                                                                              |
| 2026-06-14 | フェーズ5完了：LLM施策提案プロンプト実装。MVP（分析＋提案）が一通り完成                                                                                                                                                                                                                                                           |
| 2026-06-14 | フェーズ7（任意）追加：継続学習・ドリフト検知＋強化学習/バンディットによる施策改善                                                                                                                                                                                                                                                        |
| 2026-06-16 | 実テーブル定義を反映：CPSS→CSPM 修正、来店=FLOW_COUNTER 入退室、天候=FLOW_COUNTER_DAILY_WEATHER、SNS/外部人流は提供データに無いことを明記                                                                                                                                                                                                  |
| 2026-07-03 | 実データ実物（test_data）のEDA結果を反映：購買187店/2.44M伝票、通行人は単店140日。**来店↔購買が店舗単位でリンク不可**を明記し、実データフェーズを A(購買)/B(通行人) の2系統に整理。天候は実物未提供（定義のみ）。フェーズ6を取込/EDA・A系統・B系統・基盤に細分化                                                                                                                                           |
| 2026-07-03 | フェーズ6完了：`src/real_data_etl.py`（店舗×日／端末×日への集計）＋ `phase6_real_eda.ipynb` を実装・実行。連続日次60日以上の店舗86/187、来店↔純売上相関0.91、曜日/祝日効果を確認。返品(負値)を純売上計算で調整。方針：店舗リンクは後日確認・天候は当面なしで進める                                                                                                                                |
| 2026-07-03 | フェーズ6.5完了：86店プール（2024Q4）で購買来店予測 MAPE約29%（繁忙日約8%）、ベースライン移動平均37%を上回る。`src/real_visit_forecast.py` / `phase6_5_real_forecast.ipynb`                                                                                                                                                                  |
| 2026-07-07 | 検証期間を複数比較：通常期（10月後半〜12月前半）はMAPE 8〜15%、年末（12月後半）はMAPE 29%まで悪化。原因は学習データに年末の急落パターンが無いことと特定。`compare_validation_periods` を追加                                                                                                                                                                          |
| 2026-07-07 | 疎な101店舗（連続日次でない店舗）の活用方法を追加：B案「カレンダーのみモデル」（全187店・MAPE 8.7%）、A案「直近実績ラグ＋空白日数（gap_days）」（全187店・学習行数+37%・MAPE 9.8%）を実装し、密店舗限定（従来）と比較。特徴量重要度は店舗ID・曜日が支配的で `gap_days` 自体の寄与は小さいことを確認。`train_calendar_only_model` / `train_asof_model` / `compare_coverage_strategies` を `src/real_visit_forecast.py` に追加 |
| 2026-07-11 | 本番活用の基本仕様を決定：Webアプリで日次/週次/月次予測の選択実行＋施策提案AIチャット。推奨機能（店舗選択・実績比較・異常アラート等）とフェーズ6.9の進め方を12章に追加。フェーズ表の6.9を「Webアプリ（本番UI）」に更新                                                                                                                                                                             |
| 2026-07-11 | 技術スタック推奨案を12.5に追加：**Next.js + FastAPI**（本命）。予測は既存sklearn、データは当面Parquet/CSV→本番化でPostgreSQL。Streamlitは最速デモ用の代替。確認待ち                                                                                                                                                                                 |
| 2026-07-11 | 技術スタックを **案A（Next.js + FastAPI）で確定**。12.5を「決定」に更新                                                                                                                                                                                                                                                 |
| 2026-07-11 | フェーズ6.9 Step1完了：FastAPIで `/health` `/shops` `/predict`（日次/週次/月次）`/chat` を実装。既存 `real_visit_forecast` / `llm_advisor` をラップ。起動時にモデル学習キャッシュ                                                                                                                                                          |
| 2026-07-11 | フェーズ6.9 Step2完了：Next.js 最小UI（`web/`）。日次/週次/月次ボタン・店舗選択・予測グラフ/表・施策提案チャット                                                                                                                                                                                                                            |
| 2026-08-16 | **任意期間の予測に対応**：`/predict` が学習期間外の日付も受け付けるようにし、日付ごとに `lag` / `calendar` / `calendar_dow` を自動選択（レスポンスの `daily[].model` で明示）。学習データに無い月は月・週番号を外さないと大きく下振れすることを検証し（日曜実績平均50に対し21）、`calendar_dow` を分離。同じ月日の過去実績を前年→3年前まで遡って参考値として併記（`ref_date` / `ref_receipts`）。参照日の曜日をUI・LLMプロンプトに明示。仕様を **12.7** に新設し、12.1 に機能Cを追加 |
| 2026-08-14 | **SNS情報の使い道を決定**：過去分が遡れないため学習用特徴量には使わず、イベント情報として**LLM施策提案のコンテキスト**に注入する方針を 5.1⑤ に明記（判断根拠として実データの特徴量重要度・年末MAPEを併記）。イベント情報のデータ構造・選定ルール・プロンプト設計・将来の学習利用に向けた `collected_at` の位置づけを **5.3** に新設。フェーズ表に 6.95（設計済・未実装）を追加。確認事項に「イベント情報の収集・運用体制」「社内確定イベントの過去分提供」を追加                                        |


